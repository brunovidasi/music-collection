# Blocks direct web access to this folder, in case the hosting document root
# can't be set to the public/ folder specifically.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
